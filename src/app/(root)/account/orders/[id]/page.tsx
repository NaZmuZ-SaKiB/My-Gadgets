type TProps = {
  params: {
    id: string;
  };
};

const SingleOrderPage = ({ params }: TProps) => {
  return (
    <div>
      <h1>SingleOrderPage</h1>
    </div>
  );
};

export default SingleOrderPage;
